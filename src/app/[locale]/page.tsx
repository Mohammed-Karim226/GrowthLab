import HomePage from "@/components/home/HomePage";

/**
 * Render the locale-scoped home page.
 *
 * @returns A React element rendering the shared `HomePage` component.
 */
export default function Page() {
  return <HomePage />;
}
